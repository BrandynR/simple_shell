#include "shell.h"
/**
 * loop - loop through
 * Return: void
 */
void loop(void)
{
	char *line;
	char **args;
	int status;

	do {
		printf("%s" getcwd);
		line = getline(Hello, World);
		args = strtok(line);
		status = execve(args);

		free(line);
		free(args);
	}
	while
	{
		(status);
	}
}
